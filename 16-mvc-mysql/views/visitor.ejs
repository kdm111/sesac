<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>방명록</title>
  
</head>
<body>
  
  <form name="visitor-form">
    <fieldset>
      <legend>방명록 등록</legend>
      <input type="text" id="name" placeholder="사용자 이름" /> <br />
      <input type="text" id="comment" placeholder="방명록" /> <br />
      <div id="button-group">
      <button type="button" onclick="createVisitor()">등록</button>
      </div>
    </fieldset>
  </form>
  <table>
    <thead>
      <th>ID</th>
      <th>작성자</th>
      <th>방명록</th>
      <th>수정</th>
      <th>삭제</th>
    </thead>
    <tbody>
      <% for (let visitor of data) { %>
        <tr>
          <td><%= visitor.id %> </td>
          <td><%= visitor.name %> </td>
          <td><%= visitor.comment %> </td>
          <td><button onclick="editVisitor()">수정</button> </td>
          <td>
            <button onclick="deleteVisitor(this, '<%- visitor.id %>')">삭제</button>
          </td>
        </tr>
      <% } %>
    </tbody>
  </table>
  <a href="/">HOME</a>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <script src="/js/index.js"></script>
</body>
</html>