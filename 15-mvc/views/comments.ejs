<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Comments</title>
</head>
<body>
  <h1>댓글 목록 확인</h1>
  <a href="/">홈으로 이동</a>
  <ul>
    <% for (let comment of comments) { %>
      <li><%= comment.userid %> : <a href= "/comment/<%= comment.id%>" ><%= comment.comment %></a></li>
    <% } %>
  </ul>
</body>
</html>

